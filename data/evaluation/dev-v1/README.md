# dev-v1 历史快照

2026-09-19 异常站点修订前的原始语料保存在 `dataset.snapshot.json`，其 SHA-256 与本目录原 `qrels.json` 的 dataset 哈希一致。qrels 原件未修改；其中 dataset.path 记录的是当时的工作路径，现在该路径已更新为 v2 内容。复核旧版本时读取本目录快照，不能拿当前 `data/dev/demo.json` 静默替代。

原 qrels SHA-256：`c0175226433332e1ac592e0850248fe7bd4dbde9173d3c8f0773c2d1602a245e`。

发现的历史冲突：Q001–C002 保存为 0，理由及已确认的“同产品足以相关”规则要求 1；因此该原件实际正例数为 3 / 1 / 1，与原计划记录的 4 / 1 / 1 不一致。用户在本次修订中确认恢复为 1，修正保存在 [v2 草稿](../dev-v2/qrels.json)。保留此原件用于审计，不再作为当前正式评估输入。

此快照缺少新的必填异常站点字段，不能直接交给新版 demo；保留快照不表示提供旧模型运行兼容层。当前状态以 [Current Plan](../../../docs/project/current-plan.md) 为准。

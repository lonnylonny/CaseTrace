# dev-v2 迁移与确认记录

用户已于 **2026-09-19 最终确认 dev-v2 数据版本及全部 18 对二值标签**，`qrels.json` 状态为 `human_confirmed`，本版用于 Development 评估。确认仅同步元数据，未修改标签、理由或来源哈希。evaluator 和正式结果已交付并通过 M2 验收；旧版归档的溯源问题及用户后续决定见下方记录。

## 相对 v1 的变化

| 对象 | 修改 |
|---|---|
| C001、C003、C004 | abnormal_processes = [P004]，Wire Bond |
| C002、C006 | abnormal_processes = [P013]，Storage & Transportation |
| C005 | abnormal_processes = [P007]，Molding |
| E002 | 补充入库前留样正常、仓储防潮管控失效、出库及客退样品同类氧化的模拟调查事实，支持 C002 的仓储异常站点 |
| C002 来源记录 | simulation_revision 明确标记新增模拟事实及 E002 来源；该元数据不进入索引 |
| G001 | 显式关联 C001、C003、C004，类型为 same_abnormal_process；共同工序为 P004，不自动认定复发或生成 qrels |
| Q001–C002 | 按本轮用户确认，由 0 修正为 1；依据同产品，不依赖同站点 |

三条 Query 的文本、查询时点及 CaseDetail 均保持原样。迁移时记录其余 17 个标签与理由沿用 v1；当前旧文件的理由差异见下方核对记录。正例数为 Q001 / Q002 / Q003 = **4 / 1 / 1**。工序名称和 ID 进入检索文档，E002 内容也变化，因此后续各方法须基于此同一新版本重跑；不沿用旧排名或跨版本声称提升。

主数据 Excel 未修改。`qrels.json` 绑定当前 `data/dev/demo.json` 及 Excel 的 SHA-256，并记录旧 qrels 原件哈希。旧语料与原标注见 [v1 快照说明](../dev-v1/README.md)。

## 确认范围

用户本次确认覆盖新数据版本及全部 18 对标签，无需重复审阅确认。数据自身及各源 Case 的 draft 审阅状态保持原样，不因 qrels 确认而自动升级。当前修订延续既有模拟历史快照：案例完整内容在三条 Query 的 2026-09-15 时点之前可用；不代表已经实现通用时间过滤。

同站点只作为结合已知异常和背景的判断依据，不是独立 Relevant 充分条件；不得从历史站点向 Query 补入当前尚未知的信息。项目进度和下一交付见 [Current Plan](../../../docs/project/current-plan.md)。

## 2026-09-19 评估输入核对

- 3 个唯一 Query ID × 6 个唯一 Case ID，恰好 18 对，配对完整、无重复、无未知 ID；全部标签为整数 0 / 1，无未标注、Ambiguous 或无正例 Query。
- dataset 与主数据 Excel 的实际 SHA-256 均与 v2 的 `sources` 一致；dataset 和 qrels 均为 `development`。旧 dataset 快照也匹配 v1 记录的哈希。
- **旧 qrels 溯源不一致，尚未解决：** `data/evaluation/dev-v1/qrels.json` 的实际 SHA-256 为 `e92e5bb1f84d0e69b3a05753664b57b689bf330059c6a11cf1ccd4194e9fe933`，但 `previous_qrels.sha256` 及迁移测试记录的是 `c0175226433332e1ac592e0850248fe7bd4dbde9173d3c8f0773c2d1602a245e`。迁移测试实际在该断言失败，未刷新哈希或改写旧文件。
- 当前 v1 与 v2 除已记录的 Q001–C002 修正外，Q003–C006 的 `rationale` 也不同：v1 结尾为“情况相近”，v2 为“依据已知现象判断，不推定当前最终原因。”；该配对两版标签均为 1。仅在内存中替换这一短语仍不能复现记录的旧哈希，不能断言这就是全部字节差异。

活动 v2 的确认与上述归档问题分别记录。不得静默刷新哈希、修改已确认标签或宣称迁移校验通过。

## 2026-09-22 用户后续决定

用户说明曾人工修改 dev-v1 的 qrels，并认为可能与当时 AI 运行交错有关。这提供了哈希差异的线索，但具体时序及完整修改内容未核实，不能据此宣称旧原件已恢复。

用户随后明确“后续按照现有版本就可以”：后续开发以当前 dev-v2 数据、qrels 和已确认二值标签为准。旧 dev-v1 差异作为已知历史溯源限制保留，不再将追查或恢复旧原件作为 M2 收尾或后续开发前提。原文件、预期哈希和失败测试均保留；M2 accepted 不表示旧版迁移校验通过或全套测试全绿。

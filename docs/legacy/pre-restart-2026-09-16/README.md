# 历史规划索引

旧 Stage 1–7、旧审计、数据定义副本及旧 Agent 入口已被当前规划替代；原文保存在 Git 提交 `a1437fde45df29c19181225b407b54d8316f8bd2` 的本目录中，可用 `git show <commit>:<path>` 按需查看。

当前范围与状态使用 [Current Plan](../../project/current-plan.md)，字段与业务规则使用 [docs/data](../../data/)，协作规则使用 [AGENTS.md](../../../AGENTS.md)。旧材料不作为执行要求。
